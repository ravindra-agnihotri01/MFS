package oops.concepts.interfaceExample;

public interface Manager {
    /**
     * by default all methods are abstract
     * same was we extend class, we use implements in interface
     * interface methods and variables are by default public static final
     *
     * interface object cannot create
     */
    int appraisal= 10;

    void testcase();
    void delivery();
    void scrum();


}
